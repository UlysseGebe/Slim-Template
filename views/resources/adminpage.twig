{% include('partial/header.twig') %}

<div class="boot">
    <h1><a href="{{ path_for('categorie') }}">Catégorie</a></h1>
    <h1><a href="{{ path_for('video') }}">Vidéo</a></h1>
</div>

{% include('partial/footer.twig') %}